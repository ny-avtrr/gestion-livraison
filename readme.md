Etape de création de l'app :
- créer la structure de l'application : back et frontend
- créer le front avec vite et react
- créer le back (API) avec symphony :
     * composer creat-project symfony/skeleton backend, puis aller dans le dossier du back
     * composer require symfony/orm-pack | symfony/serializee-pack | --dev symfony/maker-bundle | nelmio/cors-bundle
     * statut pour les entités, à créer avec Enum PHP natif (backend enum)